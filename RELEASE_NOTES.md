### 🐟 正念木鱼 (woofish) v0.5.0 版本更新

#### ⚡ 核心优化：APK 体积极致瘦身
1. **彻底移除巨无霸依赖**：
   - 移除引入数千个无用图标类的 `material-icons-extended` 库，全面替换为精简的原生 XML 矢量图。
2. **剔除内置静态大文件**：
   - 彻底移除了内置的 `bgm.mp3` 静态音频（立减 1.23 MB 物理体积）。
3. **启用 R8 混淆与资源缩减**：
   - 开启 `isMinifyEnabled` 与 `isShrinkResources`，配合专业混淆规则剔除所有无用类与未引用资源，体积大幅暴降。

#### 🎵 全新特性：本地自定义背景音乐（纯应用内播放）
1. **自主选择本地音频**：
   - 点击左上角音乐图标直接唤起系统音频选择器，自由设定任意本地手机音乐（MP3 / WAV / FLAC / M4A 等）作为禅修背景音。
2. **纯净无跳转的应用内播放**：
   - 基于原生 `MediaPlayer` 在本应用内部独立循环播放、暂停与音量控制，**绝不拉起或跳出到第三方播放器**。
3. **零敏感权限 & 永久授权记忆**：
   - 基于 Android 推荐的标准 SAF 框架，无需申请读取所有文件的危险敏感权限；
   - 永久记忆所选音频，下次打开应用无感自动恢复。
4. **设置弹窗管理升级**：
   - 设置界面实时显示当前背景音乐名称，支持便捷“更换音乐”与“清除音乐”。
