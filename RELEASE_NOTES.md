### 🐟 正念木鱼 (woofish) v0.4.1 版本更新

#### ✨ 优化与体验改进
1. **🚀 默认开启全屏敲击模式**：
   - 首次安装与每次启动 APP 时，默认开启全屏敲击有效模式，手指点击屏幕任意空白区域均可轻松敲击木鱼。

2. **🧭 顶栏按键顺序优化**：
   - 调整顶栏右侧按键顺序为：`[⏱️ 自动敲击]` $\to$ `[👁️ 清屏开关]` $\to$ `[⚙️ 软件设置]`。
   - 更加贴合单手操作与高频使用习惯。

3. **🔑 持久化数字签名支持**：
   - 内置统一固定的发布签名密钥库（`woofish.keystore`），后续所有新版本升级均支持直接一键静默覆盖安装。
