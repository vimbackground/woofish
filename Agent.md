# 🤖 正念木鱼 (woofish) AI Agent 开发与维护规范

本文档为后续所有参与本项目开发的 AI Agent / 开发者制定统一的架构规范、UI 交互约定、版本演进与发布准则。

---

## 📌 1. 项目基础与技术架构

- **项目名称**：`正念木鱼` (woofish)
- **应用包名 / 命名空间**：`com.woofish`（严禁修改为无点号的单单词，必须符合 Android Package 规范）
- **核心语言与框架**：Kotlin 2.0 + Jetpack Compose (Material 3)
- **音频引擎**：
  - 点击音效：`SoundPool`（零延迟、支持高频快速敲击并发）
  - 背景音乐：`MediaPlayer`（循环播放、音量调节）
- **触感引擎**：Android 原生 `Vibrator`（`EFFECT_CLICK` 敲击质感）
- **代码结构**：极简双层结构 `app/src/main/java/com/woofish/`，禁止引入不必要的冗余深层子目录。

---

## 🧭 2. UI 布局与交互规范

### 顶栏按键分布准则
顶栏高度严格锁定为 `60.dp`，垂直居中对齐，严禁在任何状态切换时产生纵向 Y 轴位移跳动：

```
┌─────────────────────────────────────────────────────────────────┐
│ 【左侧功能区】                           【右侧控制区】           │
│  [🎵 BGM]  [✨ 动效]  [🔊 音效 1/2]        [👁️ 清屏]  [⚙️ 设置]  │
└─────────────────────────────────────────────────────────────────┘
```
- **左侧**：`[🎵 BGM 开关]` $\to$ `[✨ 动效开关]` $\to$ `[🔊 音效选择胶囊]`
- **右侧**：`[👁️ 清屏开关]` $\to$ `[⚙️ 调节设置]`（**设置按钮必须始终位于顶栏最右侧**）

### 清屏模式 (Zen Mode) 规范
- 点击【清屏】后，**顶栏所有按钮全部完整保留常驻**，支持随心调整。
- **仅清除**屏幕中央的功德数字与“功德”文字标签。

### 全屏敲击模式规范
- 在设置弹窗中提供“全屏敲击模式”开关（保存至 SharedPreferences）。
- 开启后，点击屏幕任意空白区域均可触发木鱼敲击音效、计数、震动与形变动效。

---

## 🚨 3. 核心开发守则（强制执行）

> [!IMPORTANT]
> **【永久规则】后续每次升级版本，默认必须同步更新 `README.md` 文档，以后无需用户再次提醒！**
> 
> 每次版本升级时的标准检查清单：
> 1. ✅ **`app/build.gradle.kts`**：递增 `versionCode` 并更新 `versionName`（如 `0.3.0`）。
> 2. ✅ **`.github/workflows/build-apk.yml`**：同步更新默认构建 fallback 版本号（如 `v0.3.0`）。
> 3. ✅ **`README.md`**：同步更新新版本特性说明、发布命令与下载文件名（`woofish-vX.Y.Z.apk`）。
> 4. ✅ **Git Tag**：本地打上对应标签（`git tag -f vX.Y.Z`）。

---

## 📦 4. CI/CD 与 Release 自动化规范

- GitHub Actions 工作流文件：`.github/workflows/build-apk.yml`
- 触发方式：推送到主分支或推送 `v*` 标签。
- 发布产物命名：统一为 **`woofish-v<version>.apk`**。
